'use client'
import type { Product } from '@/types'
interface Props { product: Product; onClose: () => void }
export function SizeMiniGuide({ product: _product, onClose: _onClose }: Props) { return null }
