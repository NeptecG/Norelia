# Greek i18n (next-intl) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Greek/English bilingual support to Norelia using next-intl, with Greek as the default locale (no URL prefix) and English at `/en/`.

**Architecture:** Install next-intl, move all routes under `src/app/[locale]/`, create `messages/el.json` + `messages/en.json`, update every component to use `useTranslations` / `getTranslations`, add a LanguageSwitcher with real flag SVGs to the navbar, and switch the display font between Bebas Neue (Latin) and Oswald (Greek) via a CSS variable.

**Tech Stack:** next-intl, Next.js 15 App Router, TypeScript strict, Tailwind CSS, next/font/google (Oswald added), Vitest + Testing Library.

---

## File Structure

**New files:**
- `src/middleware.ts` — next-intl locale routing
- `src/i18n.ts` — next-intl server config
- `src/navigation.ts` — typed locale-aware Link/useRouter/usePathname
- `messages/en.json` — all English strings
- `messages/el.json` — all Greek strings
- `src/types/next-intl.d.ts` — message type declaration for TS autocomplete
- `src/test-utils/intl-wrapper.tsx` — NextIntlClientProvider wrapper for tests
- `src/components/icons/flag-greece.tsx` — Greek flag SVG
- `src/components/icons/flag-uk.tsx` — UK flag SVG
- `src/components/layout/language-switcher.tsx` — locale toggle component
- `src/components/layout/language-switcher.test.tsx` — switcher tests
- `src/app/[locale]/layout.tsx` — locale provider + Nav/Footer shell
- `src/app/[locale]/page.tsx` — home (moved)
- `src/app/[locale]/[gender]/page.tsx` — gender listing (moved)
- `src/app/[locale]/[gender]/[category]/page.tsx` — category listing (moved)
- `src/app/[locale]/product/[id]/page.tsx` — product detail (moved)
- `src/app/[locale]/checkout/page.tsx` — checkout (moved)
- `src/app/[locale]/our-studio/page.tsx` — studio info (moved)
- `src/app/[locale]/studio/page.tsx` — garment designer (moved)
- `src/app/[locale]/size-guide/page.tsx` — size guide (moved)
- `src/app/[locale]/about/page.tsx` — about (moved)
- `src/app/[locale]/shipping/page.tsx` — shipping (moved)
- `src/app/[locale]/returns/page.tsx` — returns (moved)
- `src/app/[locale]/terms/page.tsx` — terms (moved)
- `src/app/[locale]/privacy/page.tsx` — privacy (moved)
- `src/app/[locale]/not-found.tsx` — 404 (moved)
- `src/app/[locale]/error.tsx` — error boundary (moved)
- `src/app/[locale]/loading.tsx` — loading (moved)

**Modified files:**
- `next.config.ts` — wrap with withNextIntl
- `src/app/layout.tsx` — use getLocale() for lang attr, add Oswald font variable
- `src/app/globals.css` — add html[lang] font-switching rules
- `src/types/index.ts` — add `kind` field to ToastState
- `src/stores/ui-store.ts` — add `kind` to showToast
- `src/components/layout/nav.tsx` — useTranslations + LanguageSwitcher + @/navigation
- `src/components/layout/site-footer.tsx` — useTranslations / getTranslations
- `src/components/layout/newsletter-bar.tsx` — useTranslations
- `src/components/layout/gdpr-banner.tsx` — useTranslations
- `src/components/layout/toast.tsx` — use kind field instead of string-sniffing
- `src/components/layout/side-panel.tsx` — useTranslations + @/navigation
- `src/components/layout/back-button.tsx` — useTranslations + @/navigation
- `src/components/layout/scroll-to-top.tsx` — useTranslations
- `src/components/home/hero.tsx` — useTranslations
- `src/components/home/featured-carousel.tsx` — useTranslations (viewAll + pagination)
- `src/components/products/product-card.tsx` — useTranslations + @/navigation
- `src/components/products/product-grid.tsx` — getTranslations
- `src/components/products/product-page.tsx` — useTranslations + @/navigation
- `src/components/modals/sign-in-modal.tsx` — useTranslations
- `src/components/modals/checkout-coming-soon-modal.tsx` — useTranslations
- `src/components/modals/size-mini-guide.tsx` — useTranslations
- All `*.test.tsx` files for translated components — add intl-wrapper / vi.mock

---

## Task 1: Install next-intl and update next.config.ts

**Files:**
- Modify: `next.config.ts`
- Run: `npm install next-intl` in project root

- [ ] **Step 1: Install next-intl**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm install next-intl
```

Expected: next-intl added to package.json dependencies.

- [ ] **Step 2: Update next.config.ts**

Replace the entire file:

```ts
import type { NextConfig } from 'next'
import createNextIntlPlugin from 'next-intl/plugin'

const withNextIntl = createNextIntlPlugin('./src/i18n.ts')

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'images.pexels.com' },
      { protocol: 'https', hostname: 'images.unsplash.com' },
    ],
  },
}

export default withNextIntl(nextConfig)
```

- [ ] **Step 3: Verify TypeScript still compiles**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npx tsc --noEmit
```

Expected: errors about missing `src/i18n.ts` — that is fine, will be fixed in Task 2.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add next.config.ts package.json package-lock.json && git commit -m "feat(i18n): install next-intl and wire next.config.ts"
```

---

## Task 2: Create i18n infrastructure (middleware, i18n.ts, navigation.ts)

**Files:**
- Create: `src/middleware.ts`
- Create: `src/i18n.ts`
- Create: `src/navigation.ts`

- [ ] **Step 1: Create src/i18n.ts**

```ts
import { getRequestConfig } from 'next-intl/server'

export default getRequestConfig(async ({ requestLocale }) => {
  const locale = (await requestLocale) ?? 'el'
  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  }
})
```

- [ ] **Step 2: Create src/middleware.ts**

```ts
import createMiddleware from 'next-intl/middleware'

export default createMiddleware({
  locales: ['el', 'en'],
  defaultLocale: 'el',
  localePrefix: 'as-needed',
})

export const config = {
  matcher: ['/((?!api|_next|_vercel|.*\\..*).*)'],
}
```

- [ ] **Step 3: Create src/navigation.ts**

```ts
import { createNavigation } from 'next-intl/navigation'

const routing = {
  locales: ['el', 'en'] as const,
  defaultLocale: 'el' as const,
  localePrefix: 'as-needed' as const,
}

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing)
```

- [ ] **Step 4: Verify TypeScript (errors expected until messages exist)**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/middleware.ts src/i18n.ts src/navigation.ts && git commit -m "feat(i18n): add middleware, i18n request config, and navigation helpers"
```

---

## Task 3: Update ToastState type and UIStore to add kind field

This fixes the i18n-incompatible string-sniffing in Toast component.

**Files:**
- Modify: `src/types/index.ts`
- Modify: `src/stores/ui-store.ts`

- [ ] **Step 1: Update ToastState in src/types/index.ts**

Find:
```ts
export interface ToastState {
  msg:     string
  visible: boolean
  type:    'add' | 'remove'
}
```

Replace with:
```ts
export interface ToastState {
  msg:     string
  visible: boolean
  type:    'add' | 'remove'
  kind?:   'cart' | 'fav'
}
```

- [ ] **Step 2: Update UIStore showToast signature in src/stores/ui-store.ts**

Find:
```ts
  toast:                ToastState
  showToast:            (msg: string, type: 'add' | 'remove') => void
```

Replace with:
```ts
  toast:                ToastState
  showToast:            (msg: string, type: 'add' | 'remove', kind?: 'cart' | 'fav') => void
```

Find:
```ts
      toast: { msg: '', visible: false, type: 'add' },
      showToast: (msg, type) => {
        // Cancel any in-flight hide timer so rapid toasts each get the full duration
        if (_toastTimer) { clearTimeout(_toastTimer); _toastTimer = null }
        set({ toast: { msg, visible: true, type } })
```

Replace with:
```ts
      toast: { msg: '', visible: false, type: 'add', kind: undefined },
      showToast: (msg, type, kind) => {
        // Cancel any in-flight hide timer so rapid toasts each get the full duration
        if (_toastTimer) { clearTimeout(_toastTimer); _toastTimer = null }
        set({ toast: { msg, visible: true, type, kind } })
```

- [ ] **Step 3: Run tests to confirm existing behaviour unchanged**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test -- --reporter=verbose 2>&1 | tail -20
```

Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/types/index.ts src/stores/ui-store.ts && git commit -m "feat(i18n): add kind field to ToastState for locale-safe icon detection"
```

---

## Task 4: Create messages/en.json

**Files:**
- Create: `messages/en.json`

- [ ] **Step 1: Create messages/en.json with all English strings**

```json
{
  "Nav": {
    "home": "Home",
    "men": "Men",
    "women": "Women",
    "designYourOwn": "Design Your Own",
    "signIn": "Sign In",
    "signInLabel": "Sign in to your account",
    "searchLabel": "Search products",
    "cartLabel": "Cart",
    "cartWithCount": "Cart ({count} items)",
    "favoritesLabel": "Favorites",
    "favoritesWithCount": "Favorites ({count})",
    "searchPlaceholder": "Search products...",
    "noResults": "No results for",
    "menuLabel": "Menu",
    "closePanel": "Close"
  },
  "Hero": {
    "srHeading": "NORELIA. Premium Streetwear",
    "shopEyebrow": "SHOP",
    "womenLabel": "WOMEN",
    "menLabel": "MEN",
    "womenAriaLabel": "Shop Women",
    "menAriaLabel": "Shop Men"
  },
  "Home": {
    "newInTitle": "NEW IN COLLECTION",
    "newInSubtitle": "JUST ARRIVED",
    "trendingTitle": "TRENDING",
    "trendingSubtitle": "THIS SEASON",
    "onSaleTitle": "ON SALE",
    "onSaleSubtitle": "LIMITED TIME OFFERS"
  },
  "FeaturedCarousel": {
    "viewAll": "View All",
    "goToPage": "Go to page {n}"
  },
  "ProductGrid": {
    "nonFound": "NONE FOUND",
    "comingSoon": "COMING SOON",
    "styles": "styles"
  },
  "ProductCard": {
    "addToFavorites": "Add to favorites",
    "removeFromFavorites": "Remove from favorites",
    "quickAdd": "QUICK ADD",
    "outOfStock": "OUT OF STOCK",
    "new": "NEW",
    "addedToFavorites": "Added to favorites",
    "removedFromFavorites": "Removed from favorites",
    "noMoreStock": "No more {size} left for {name}",
    "sizeAdded": "{name}, {size} added"
  },
  "ProductPage": {
    "sizeLabel": "Size",
    "addToCart": "Add to Cart",
    "addedToCart": "{name} added to cart",
    "addToFavorites": "Add to Favourites",
    "removeFromFavorites": "Remove from Favourites",
    "addedToFavorites": "{name} saved to favourites",
    "removedFromFavorites": "{name} removed from favourites",
    "soldOut": "Sold Out",
    "sizeGuide": "Size Guide",
    "free": "Free",
    "selectSize": "Select a size to add to cart",
    "inStock": "{n} left",
    "measurementsCm": "measurements in cm"
  },
  "SidePanel": {
    "cart": "MY CART",
    "favorites": "FAVORITES",
    "close": "Close panel",
    "cartEmpty": "Your cart is empty.",
    "noSaved": "No saved items yet.",
    "saveForLater": "Save for later",
    "removeFromSaved": "Remove from saved",
    "removeItem": "Remove item",
    "orderTotal": "Order Total",
    "freeShipping": "Free shipping on your order!",
    "addMoreForFreeShipping": "Add €{amount} more for free shipping",
    "checkout": "Proceed to Checkout",
    "removedFromCart": "{name} removed from cart",
    "addedToFavorites": "{name} added to favorites",
    "removedFromFavorites": "{name} removed from favorites"
  },
  "NewsletterBar": {
    "headline": "Stay in the loop.",
    "subtitle": "Early access to drops & exclusive offers",
    "placeholder": "your@email.com",
    "subscribe": "Subscribe",
    "success": "✓ You’re in."
  },
  "GDPRBanner": {
    "title": "We use cookies",
    "body": "We use cookies to improve your browsing experience and analyse site traffic. By clicking Accept All, you consent to our use of cookies in accordance with EU regulations (GDPR). You can decline non-essential cookies without affecting your shopping experience.",
    "acceptAll": "Accept All",
    "decline": "Decline",
    "acceptLabel": "Accept all cookies",
    "declineLabel": "Decline non-essential cookies"
  },
  "SiteFooter": {
    "premiumStreetware": "Premium Streetwear",
    "information": "Information",
    "about": "About Us",
    "sizeGuide": "Size Guide",
    "shipping": "Shipping",
    "returns": "Returns & Exchanges",
    "privacy": "Privacy Policy",
    "terms": "Terms of Service",
    "contact": "Contact",
    "hours": "Mon–Fri, 9:00–17:00",
    "saturday": "Saturday, 10:00–14:00",
    "ourStudio": "Our Studio",
    "payment": "Payment Methods",
    "copyright": "© {year} Norelia. All rights reserved.",
    "tagline": "Premium Streetwear · Preveza, Greece",
    "secure": "Secure Payments · SSL Encrypted",
    "instagramLabel": "Norelia on Instagram",
    "xLabel": "Norelia on X",
    "youtubeLabel": "Norelia on YouTube"
  },
  "SignInModal": {
    "signInTitle": "SIGN IN",
    "registerTitle": "CREATE ACCOUNT",
    "signInTab": "Sign In",
    "registerTab": "Create Account",
    "emailPlaceholder": "Email",
    "passwordPlaceholder": "Password",
    "namePlaceholder": "Full Name",
    "forgotPassword": "Forgot your password?",
    "signInButton": "Sign In",
    "createButton": "Create Account",
    "close": "Close",
    "comingSoon": "UI only · authentication coming soon"
  },
  "CheckoutComingSoonModal": {
    "title": "CHECKOUT",
    "body": "We’re getting the checkout experience ready. Thank you for your patience.",
    "close": "Close"
  },
  "CheckoutPage": {
    "title": "My Shopping Cart"
  },
  "ScrollToTop": {
    "label": "Scroll to top",
    "top": "TOP"
  },
  "BackButton": {
    "label": "← Back"
  },
  "NotFound": {
    "heading": "Page Not Found",
    "body": "The page you’re looking for doesn’t exist.",
    "back": "Back to {brand}"
  },
  "About": {
    "eyebrow": "Est. 2024 · Preveza, Greece",
    "heading": "About\n{brand}",
    "introTitle": "Born in the West of Greece.",
    "intro1": "{brand} started the way most things worth doing start, with frustration. We couldn’t find streetwear that felt both premium and personal, both sharp and wearable in the heat of a Greek summer. So we made our own.",
    "intro2": "Founded in Preveza in 2024, {brand} is a small independent label built around one idea: that where you’re from shouldn’t limit what you wear. We design pieces that can move from the streets of a quiet Greek town to the front row of anything, no apology required.",
    "intro3": "Everything is designed in-house. Every drop is intentional. We don’t chase trends. We make things we’d actually wear, and trust that the right people will find them.",
    "pillar01Title": "Design First",
    "pillar01Body": "Every piece starts with a sketch, not a sales target. We design for the garment, then figure out the rest.",
    "pillar02Title": "Made to Last",
    "pillar02Body": "We use heavyweight, high-quality fabrics because fast fashion isn’t something we believe in. Buy less, wear more.",
    "pillar03Title": "Your Way",
    "pillar03Body": "Our studio lets you customise any piece: colours, prints, placement. If you’re going to wear something, it should be yours.",
    "closingTitle": "Still just getting started.",
    "closingBody": "{brand} is young, intentionally small, and not in a hurry. We’d rather get it right than get it fast. Follow along. The best is still ahead."
  },
  "OurStudio": {
    "eyebrow": "{brand} Premium Streetwear",
    "heading": "Our Studio",
    "addressLabel": "Address",
    "address1": "G. Gianniwth 216",
    "address2": "Preveza 48100",
    "address3": "Greece",
    "hoursLabel": "Studio Hours",
    "monFri": "Mon – Fri",
    "saturday": "Saturday",
    "sunday": "Sunday",
    "monFriHours": "09:00 – 17:00",
    "saturdayHours": "10:00 – 14:00",
    "closed": "Closed",
    "contactLabel": "Get in Touch",
    "mapsLink": "Open in Google Maps ↗",
    "mapTitle": "Norelia studio location, Preveza, Greece",
    "strip": "{brand} Premium Streetwear · Preveza, Greece"
  },
  "Shipping": {
    "eyebrow": "Delivery",
    "heading": "SHIPPING",
    "intro": "We ship every NORELIA. order with care. Whether you’re across town or across borders, here’s everything you need to know before and after you place your order.",
    "method1Title": "Standard Shipping",
    "method1Time": "3–5 Business Days",
    "method1Price": "Free on orders over €{threshold} · €4.90 below",
    "method1Detail": "Available for all destinations within Greece. Orders are processed within 1–2 business days and dispatched via ACS Courier or ELTA. You’ll receive a tracking number by email once your order ships.",
    "method2Title": "Express Shipping",
    "method2Time": "1–2 Business Days",
    "method2Price": "€8.90",
    "method2Detail": "Need it faster? Select express at checkout for next-day or 2-day delivery within Greece. Orders placed before 13:00 are dispatched same day.",
    "method3Title": "International Shipping",
    "method3Time": "5–12 Business Days",
    "method3Price": "From €12.90",
    "method3Detail": "We ship to all EU countries and select international destinations. Delivery times and rates vary by destination. Customs duties and import taxes may apply and are the responsibility of the recipient.",
    "noteStrong": "Please note: ",
    "note": "Delivery times are estimates and may vary during peak periods, public holidays, or due to circumstances beyond our control. Once an order has been dispatched, NORELIA. is not responsible for delays caused by the courier."
  },
  "Returns": {
    "eyebrow": "Policy",
    "heading": "RETURNS &\nEXCHANGES",
    "intro": "We want you to love what you ordered. If something isn’t right, here’s how we make it right.",
    "section1Title": "Return Policy",
    "section1Detail": "You have 14 days from the date of delivery to request a return. Items must be unworn, unwashed, and in their original condition with all tags attached. Sale items and customised pieces from our Design Your Own studio are final sale and cannot be returned.",
    "section2Title": "How to Return",
    "section2Detail": "Email us at hello@norelia.com with your order number and reason for return. We’ll respond within 1–2 business days with a return authorisation and instructions. Once we receive and inspect the item, your refund will be processed within 5–7 business days to your original payment method.",
    "section3Title": "Exchanges",
    "section3Detail": "Want a different size or colour? We offer free exchanges on full-price items within 14 days of delivery. Simply contact us at hello@norelia.com and we’ll sort it out. Exchanges are subject to stock availability.",
    "section4Title": "Damaged or Incorrect Items",
    "section4Detail": "If your order arrives damaged or you received the wrong item, please contact us within 48 hours of delivery with photos. We’ll arrange a replacement or full refund at no cost to you. No questions asked.",
    "noteStrong": "Return shipping costs ",
    "note": "are the responsibility of the customer unless the item is faulty or incorrect. We recommend using a tracked service as we cannot be held responsible for returns lost in transit."
  },
  "Terms": {
    "eyebrow": "Legal",
    "heading": "TERMS OF\nSERVICE",
    "section1Title": "Acceptance of Terms",
    "section1Detail": "By placing an order on this website you confirm that you are at least 18 years old and agree to these terms in full. These terms are governed by Greek law and any disputes will be subject to the exclusive jurisdiction of the courts of Preveza, Greece.",
    "section2Title": "Orders & Pricing",
    "section2Detail": "All prices are displayed in Euros (€) and include VAT where applicable. We reserve the right to refuse or cancel any order, including after confirmation, if we detect fraud, pricing errors, or stock discrepancies. In such cases you will receive a full refund. Promotional codes are single-use and cannot be combined.",
    "section3Title": "Custom Orders",
    "section3Detail": "Orders placed through the Design Your Own studio are made to order. By uploading your artwork you confirm you own the rights to use it commercially and that it does not infringe third-party intellectual property. NORELIA. accepts no liability for content uploaded by customers.",
    "section4Title": "Intellectual Property",
    "section4Detail": "All designs, graphics, product names, and brand assets on this site are the exclusive property of {brand} and may not be reproduced, copied, or distributed without written permission. The site software and structure are also protected by copyright.",
    "section5Title": "Limitation of Liability",
    "section5Detail": "{brand}’s total liability for any claim arising from a purchase is limited to the value of the order in question. We are not liable for indirect or consequential losses. We do not guarantee uninterrupted access to this website.",
    "section6Title": "Changes to These Terms",
    "section6Detail": "We may update these terms at any time. The version in force at the time of your order applies to that order. Continued use of the site after a terms update constitutes acceptance of the new terms. This version was last updated in May 2026."
  },
  "Privacy": {
    "eyebrow": "Legal",
    "heading": "PRIVACY\nPOLICY"
  },
  "SizeGuide": {
    "heading": "SIZE GUIDE",
    "eyebrow": "Fit",
    "intro": "All NORELIA. garments are sized XS through XL. Measure yourself and use the table below to find your best fit.",
    "measureTitle": "How to measure",
    "measureChest": "Chest: Measure horizontally around the fullest part of your chest.",
    "measureWaist": "Waist: Measure around the narrowest part of your torso.",
    "measureHip": "Hips: Measure around the fullest part.",
    "menTitle": "Men",
    "womenTitle": "Women",
    "tshirtsTab": "T-SHIRTS",
    "hoodiesTab": "HOODIES",
    "zippersTab": "ZIPPERS",
    "studioLink": "Design your own →"
  },
  "GenderPage": {
    "men": "MEN",
    "women": "WOMEN",
    "styles": "styles"
  }
}
```

- [ ] **Step 2: Run TypeScript**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add messages/en.json && git commit -m "feat(i18n): add English message file with all UI strings"
```

---

## Task 5: Create messages/el.json

**Files:**
- Create: `messages/el.json`

- [ ] **Step 1: Create messages/el.json**

Note: Greek uppercase strings have NO accent marks (tonos removed — correct Greek typography). Mixed-case strings retain accents.

```json
{
  "Nav": {
    "home": "ΑΡΧΙΚΗ",
    "men": "ΑΝΔΡΑΣ",
    "women": "ΓΥΝΑΙΚΑ",
    "designYourOwn": "ΣΧΕΔΙΑΣΕ ΤΟ ΔΙΚΟ ΣΟΥ",
    "signIn": "ΣΥΝΔΕΣΗ",
    "signInLabel": "Σύνδεση στον λογαριασμό σου",
    "searchLabel": "Αναζήτηση προϊόντων",
    "cartLabel": "Καλάθι",
    "cartWithCount": "Καλάθι ({count} προϊόντα)",
    "favoritesLabel": "Αγαπημένα",
    "favoritesWithCount": "Αγαπημένα ({count})",
    "searchPlaceholder": "Αναζήτηση προϊόντων...",
    "noResults": "Δεν βρέθηκαν αποτελέσματα για",
    "menuLabel": "Μενού",
    "closePanel": "Κλείσιμο"
  },
  "Hero": {
    "srHeading": "NORELIA. Premium Streetwear",
    "shopEyebrow": "SHOP",
    "womenLabel": "ΓΥΝΑΙΚΑ",
    "menLabel": "ΑΝΔΡΑΣ",
    "womenAriaLabel": "Shop γυναικεία",
    "menAriaLabel": "Shop ανδρικά"
  },
  "Home": {
    "newInTitle": "ΝΕΑ ΣΥΛΛΟΓΗ",
    "newInSubtitle": "ΜΟΛΙΣ ΕΦΤΑΣΕ",
    "trendingTitle": "TRENDING",
    "trendingSubtitle": "ΤΗ ΣΕΖΟΝ",
    "onSaleTitle": "ΣΕ ΕΚΠΤΩΣΗ",
    "onSaleSubtitle": "ΠΕΡΙΟΡΙΣΜΕΝΕΣ ΠΡΟΣΦΟΡΕΣ"
  },
  "FeaturedCarousel": {
    "viewAll": "Δες Όλα",
    "goToPage": "Σελίδα {n}"
  },
  "ProductGrid": {
    "nonFound": "ΚΑΝΕΝΑ ΑΠΟΤΕΛΕΣΜΑ",
    "comingSoon": "ΕΡΧΕΤΑΙ ΣΥΝΤΟΜΑ",
    "styles": "στυλ"
  },
  "ProductCard": {
    "addToFavorites": "Προσθήκη στα αγαπημένα",
    "removeFromFavorites": "Αφαίρεση από τα αγαπημένα",
    "quickAdd": "ΓΡΗΓΟΡΗ ΠΡΟΣΘΗΚΗ",
    "outOfStock": "ΕΞΑΝΤΛΗΘΗΚΕ",
    "new": "ΝΕΟ",
    "addedToFavorites": "Προστέθηκε στα αγαπημένα",
    "removedFromFavorites": "Αφαιρέθηκε από τα αγαπημένα",
    "noMoreStock": "Δεν υπάρχει άλλο {size} για {name}",
    "sizeAdded": "{name}, {size} προστέθηκε"
  },
  "ProductPage": {
    "sizeLabel": "Μέγεθος",
    "addToCart": "Προσθήκη στο Καλάθι",
    "addedToCart": "{name} προστέθηκε στο καλάθι",
    "addToFavorites": "Προσθήκη στα Αγαπημένα",
    "removeFromFavorites": "Αφαίρεση από τα Αγαπημένα",
    "addedToFavorites": "{name} αποθηκεύτηκε στα αγαπημένα",
    "removedFromFavorites": "{name} αφαιρέθηκε από τα αγαπημένα",
    "soldOut": "Εξαντλήθηκε",
    "sizeGuide": "Οδηγός Μεγεθών",
    "free": "Δωρεάν",
    "selectSize": "Επίλεξε μέγεθος για να προσθέσεις στο καλάθι",
    "inStock": "{n} διαθέσιμα",
    "measurementsCm": "μετρήσεις σε cm"
  },
  "SidePanel": {
    "cart": "ΤΟ ΚΑΛΑΘΙ ΜΟΥ",
    "favorites": "ΑΓΑΠΗΜΕΝΑ",
    "close": "Κλείσιμο",
    "cartEmpty": "Το καλάθι σου είναι άδειο.",
    "noSaved": "Δεν έχεις αποθηκευμένα προϊόντα.",
    "saveForLater": "Αποθήκευση",
    "removeFromSaved": "Αφαίρεση από αποθηκευμένα",
    "removeItem": "Αφαίρεση προϊόντος",
    "orderTotal": "Σύνολο Παραγγελίας",
    "freeShipping": "Δωρεάν αποστολή!",
    "addMoreForFreeShipping": "Πρόσθεσε €{amount} για δωρεάν αποστολή",
    "checkout": "Προχώρηση στην Αγορά",
    "removedFromCart": "{name} αφαιρέθηκε από το καλάθι",
    "addedToFavorites": "{name} προστέθηκε στα αγαπημένα",
    "removedFromFavorites": "{name} αφαιρέθηκε από τα αγαπημένα"
  },
  "NewsletterBar": {
    "headline": "Μείνε ενήμερος.",
    "subtitle": "ΠΡΩΤΗ ΠΡΟΣΒΑΣΗ ΣΕ DROPS ΚΑΙ EXCLUSIVE ΠΡΟΣΦΟΡΕΣ",
    "placeholder": "email@σου.com",
    "subscribe": "ΕΓΓΡΑΦΗ",
    "success": "✓ Είσαι μέσα."
  },
  "GDPRBanner": {
    "title": "Χρησιμοποιούμε cookies",
    "body": "Χρησιμοποιούμε cookies για να βελτιώσουμε την εμπειρία περιήγησής σου και να αναλύσουμε την επισκεψιμότητα. Πατώντας Αποδοχή Όλων, συναινείς στη χρήση cookies σύμφωνα με τους κανονισμούς της ΕΕ (GDPR). Μπορείς να αρνηθείς τα μη απαραίτητα cookies χωρίς να επηρεαστεί η εμπειρία αγορών σου.",
    "acceptAll": "Αποδοχή Όλων",
    "decline": "Απόρριψη",
    "acceptLabel": "Αποδοχή όλων των cookies",
    "declineLabel": "Απόρριψη μη απαραίτητων cookies"
  },
  "SiteFooter": {
    "premiumStreetware": "Premium Streetwear",
    "information": "Πληροφορίες",
    "about": "Σχετικά με εμάς",
    "sizeGuide": "Οδηγός Μεγεθών",
    "shipping": "Αποστολή",
    "returns": "Επιστροφές και Ανταλλαγές",
    "privacy": "Πολιτική Απορρήτου",
    "terms": "Όροι Χρήσης",
    "contact": "Επικοινωνία",
    "hours": "Δευ-Παρ, 9:00-17:00",
    "saturday": "Σάβ, 10:00-14:00",
    "ourStudio": "Το Studio Μας",
    "payment": "Τρόποι Πληρωμής",
    "copyright": "© {year} Norelia. Με την επιφύλαξη παντός δικαιώματος.",
    "tagline": "Premium Streetwear · Πρέβεζα, Ελλάδα",
    "secure": "Ασφαλείς Πληρωμές · SSL Κρυπτογράφηση",
    "instagramLabel": "Norelia στο Instagram",
    "xLabel": "Norelia στο X",
    "youtubeLabel": "Norelia στο YouTube"
  },
  "SignInModal": {
    "signInTitle": "ΣΥΝΔΕΣΗ",
    "registerTitle": "ΔΗΜΙΟΥΡΓΙΑ ΛΟΓΑΡΙΑΣΜΟΥ",
    "signInTab": "Σύνδεση",
    "registerTab": "Δημιουργία Λογαριασμού",
    "emailPlaceholder": "Email",
    "passwordPlaceholder": "Κωδικός",
    "namePlaceholder": "Ονοματεπώνυμο",
    "forgotPassword": "Ξέχασες τον κωδικό σου;",
    "signInButton": "ΣΥΝΔΕΣΗ",
    "createButton": "ΔΗΜΙΟΥΡΓΙΑ ΛΟΓΑΡΙΑΣΜΟΥ",
    "close": "Κλείσιμο",
    "comingSoon": "Μόνο UI · η σύνδεση έρχεται σύντομα"
  },
  "CheckoutComingSoonModal": {
    "title": "ΟΛΟΚΛΗΡΩΣΗ ΑΓΟΡΑΣ",
    "body": "Ετοιμάζουμε την εμπειρία αγοράς. Σας ευχαριστούμε για την υπομονή σας.",
    "close": "Κλείσιμο"
  },
  "CheckoutPage": {
    "title": "Το Καλάθι Μου"
  },
  "ScrollToTop": {
    "label": "Επιστροφή στην αρχή",
    "top": "ΚΟΡΥΦΗ"
  },
  "BackButton": {
    "label": "← Πίσω"
  },
  "NotFound": {
    "heading": "Η Σελίδα Δεν Βρέθηκε",
    "body": "Η σελίδα που ψάχνεις δεν υπάρχει.",
    "back": "Πίσω στην {brand}"
  },
  "About": {
    "eyebrow": "Ίδρ. 2024 · Πρέβεζα, Ελλάδα",
    "heading": "Σχετικά με\n{brand}",
    "introTitle": "Γεννήθηκε στη Δυτική Ελλάδα.",
    "intro1": "{brand} ξεκίνησε όπως αρχίζουν τα περισσότερα πράγματα που αξίζουν, με απογοήτευση. Δεν μπορούσαμε να βρούμε streetwear που να είναι ταυτόχρονα premium και προσωπικό, κομψό και φορέσιμο στη ζέστη ενός ελληνικού καλοκαιριού. Οπότε φτιάξαμε το δικό μας.",
    "intro2": "Ιδρύθηκε στην Πρέβεζα το 2024, η {brand} είναι μια μικρή ανεξάρτητη μάρκα χτισμένη γύρω από μια ιδέα: ότι το πού κατάγεσαι δεν πρέπει να περιορίζει το τι φοράς. Σχεδιάζουμε κομμάτια που μπορούν να ταξιδέψουν από τους δρόμους μιας ήσυχης ελληνικής πόλης στην πρώτη γραμμή οπουδήποτε.",
    "intro3": "Όλα σχεδιάζονται εσωτερικά. Κάθε κυκλοφορία είναι σκόπιμη. Δεν κυνηγάμε τάσεις. Φτιάχνουμε πράγματα που θα φορούσαμε οι ίδιοι, και εμπιστευόμαστε ότι οι σωστοί άνθρωποι θα τα βρουν.",
    "pillar01Title": "Σχέδιο Πρώτα",
    "pillar01Body": "Κάθε κομμάτι ξεκινά από σκίτσο, όχι από εμπορικό στόχο. Σχεδιάζουμε για το ρούχο, και μετά λύνουμε τα υπόλοιπα.",
    "pillar02Title": "Ανθεκτικό",
    "pillar02Body": "Χρησιμοποιούμε βαριά, υψηλής ποιότητας υφάσματα γιατί δεν πιστεύουμε στη fast fashion. Αγόρασε λιγότερο, φόρεσε περισσότερο.",
    "pillar03Title": "Με τον τρόπο σου",
    "pillar03Body": "Το studio μας σου επιτρέπει να προσαρμόσεις κάθε κομμάτι: χρώματα, εκτυπώσεις, τοποθέτηση. Αν θα το φοράς, πρέπει να είναι δικό σου.",
    "closingTitle": "Μόλις αρχίσαμε.",
    "closingBody": "{brand} είναι νέα, σκόπιμα μικρή, και δεν βιάζεται. Προτιμάμε να το κάνουμε σωστά παρά γρήγορα. Ακολούθησέ μας. Το καλύτερο έρχεται."
  },
  "OurStudio": {
    "eyebrow": "{brand} Premium Streetwear",
    "heading": "Το Studio Μας",
    "addressLabel": "Διεύθυνση",
    "address1": "Γ. Γιαννιώτη 216",
    "address2": "Πρέβεζα 48100",
    "address3": "Ελλάδα",
    "hoursLabel": "Ώρες Λειτουργίας",
    "monFri": "Δευ – Παρ",
    "saturday": "Σάββατο",
    "sunday": "Κυριακή",
    "monFriHours": "09:00 – 17:00",
    "saturdayHours": "10:00 – 14:00",
    "closed": "Κλειστά",
    "contactLabel": "Επικοινωνία",
    "mapsLink": "Άνοιγμα στο Google Maps ↗",
    "mapTitle": "Τοποθεσία studio Norelia, Πρέβεζα, Ελλάδα",
    "strip": "{brand} Premium Streetwear · Πρέβεζα, Ελλάδα"
  },
  "Shipping": {
    "eyebrow": "Αποστολή",
    "heading": "ΑΠΟΣΤΟΛΗ",
    "intro": "Αποστέλλουμε κάθε παραγγελία NORELIA. με φροντίδα. Είτε βρίσκεσαι στην πόλη είτε στο εξωτερικό, εδώ είναι όλα όσα πρέπει να ξέρεις πριν και μετά την παραγγελία σου.",
    "method1Title": "Κανονική Αποστολή",
    "method1Time": "3-5 Εργάσιμες Ημέρες",
    "method1Price": "Δωρεάν για παραγγελίες άνω των €{threshold} · €4,90 κάτω",
    "method1Detail": "Διαθέσιμη για όλους τους προορισμούς εντός Ελλάδας. Οι παραγγελίες επεξεργάζονται εντός 1-2 εργάσιμων ημερών και αποστέλλονται μέσω ACS Courier ή ΕΛΤΑ. Θα λάβεις αριθμό παρακολούθησης με email μόλις αποσταλεί η παραγγελία σου.",
    "method2Title": "Ταχεία Αποστολή",
    "method2Time": "1-2 Εργάσιμες Ημέρες",
    "method2Price": "€8,90",
    "method2Detail": "Θέλεις πιο γρήγορα; Επίλεξε express κατά την ολοκλήρωση για παράδοση την επόμενη ή σε 2 ημέρες εντός Ελλάδας. Παραγγελίες που γίνονται πριν τις 13:00 αποστέλλονται αυθημερόν.",
    "method3Title": "Διεθνής Αποστολή",
    "method3Time": "5-12 Εργάσιμες Ημέρες",
    "method3Price": "Από €12,90",
    "method3Detail": "Αποστέλλουμε σε όλες τις χώρες της ΕΕ και επιλεγμένους διεθνείς προορισμούς. Χρόνοι και χρεώσεις ποικίλλουν ανά προορισμό. Ενδέχεται να ισχύουν τελωνειακοί δασμοί που βαρύνουν τον παραλήπτη.",
    "noteStrong": "Σημείωση: ",
    "note": "Οι χρόνοι παράδοσης είναι εκτιμήσεις και μπορεί να διαφέρουν κατά τις αιχμές, τις αργίες ή λόγω περιστάσεων εκτός ελέγχου. Μόλις αποσταλεί μια παραγγελία, η NORELIA. δεν ευθύνεται για καθυστερήσεις από τον courier."
  },
  "Returns": {
    "eyebrow": "Πολιτική",
    "heading": "ΕΠΙΣΤΡΟΦΕΣ ΚΑΙ\nΑΝΤΑΛΛΑΓΕΣ",
    "intro": "Θέλουμε να αγαπάς αυτό που παρήγγειλες. Αν κάτι δεν πάει καλά, έτσι το διορθώνουμε.",
    "section1Title": "Πολιτική Επιστροφών",
    "section1Detail": "Έχεις 14 ημέρες από την παράδοση για επιστροφή. Τα προϊόντα πρέπει να επιστρέφονται αχρησιμοποίητα, άπλυτα και στην αρχική τους κατάσταση με τις ετικέτες. Τα εκπτωτικά και τα προσαρμοσμένα προϊόντα δεν επιστρέφονται.",
    "section2Title": "Πώς να Επιστρέψεις",
    "section2Detail": "Στείλε email στο hello@norelia.com με τον αριθμό παραγγελίας και τον λόγο επιστροφής. Θα απαντήσουμε εντός 1-2 εργάσιμων ημερών με οδηγίες. Η επιστροφή χρημάτων ολοκληρώνεται σε 5-7 εργάσιμες ημέρες.",
    "section3Title": "Ανταλλαγές",
    "section3Detail": "Θέλεις διαφορετικό νούμερο ή χρώμα; Προσφέρουμε δωρεάν ανταλλαγές σε προϊόντα πλήρους τιμής εντός 14 ημερών. Επικοινώνησε μαζί μας και θα φροντίσουμε για τα υπόλοιπα, βάσει διαθεσιμότητας.",
    "section4Title": "Κατεστραμμένα ή Λανθασμένα",
    "section4Detail": "Αν η παραγγελία σου παραδοθεί κατεστραμμένη ή λάβεις λανθασμένο προϊόν, επικοινώνησε μαζί μας εντός 48 ωρών με φωτογραφίες. Θα κανονίσουμε αντικατάσταση ή πλήρη επιστροφή χρημάτων χωρίς καμία χρέωση.",
    "noteStrong": "Έξοδα επιστροφής ",
    "note": "βαρύνουν τον πελάτη εκτός αν το προϊόν είναι ελαττωματικό ή λανθασμένο. Συνιστούμε αποστολή με ιχνηλάτηση καθώς δεν ευθυνόμαστε για απώλεια κατά την επιστροφή."
  },
  "Terms": {
    "eyebrow": "Νομικά",
    "heading": "ΟΡΟΙ\nΧΡΗΣΗΣ",
    "section1Title": "Αποδοχή Όρων",
    "section1Detail": "Με την υποβολή παραγγελίας επιβεβαιώνεις ότι είσαι τουλάχιστον 18 ετών και αποδέχεσαι πλήρως τους παρόντες όρους. Οι όροι διέπονται από το ελληνικό δίκαιο και τυχόν διαφορές υπάγονται στη δικαιοδοσία των δικαστηρίων της Πρέβεζας.",
    "section2Title": "Παραγγελίες και Τιμές",
    "section2Detail": "Όλες οι τιμές αναγράφονται σε Ευρώ (€) και περιλαμβάνουν ΦΠΑ όπου ισχύει. Διατηρούμε το δικαίωμα να αρνηθούμε ή να ακυρώσουμε παραγγελία αν εντοπίσουμε απάτη, λάθος τιμολόγησης ή αναντιστοιχία αποθέματος, με πλήρη επιστροφή χρημάτων. Τα κουπόνια είναι μοναδικής χρήσης.",
    "section3Title": "Προσαρμοσμένες Παραγγελίες",
    "section3Detail": "Παραγγελίες από το Design Your Own studio γίνονται κατά παραγγελία. Με την ανάρτηση έργου επιβεβαιώνεις ότι κατέχεις τα δικαιώματα εμπορικής χρήσης και δεν παραβιάζεις πνευματικά δικαιώματα τρίτων. Η NORELIA. δεν φέρει ευθύνη για περιεχόμενο που αναρτούν οι πελάτες.",
    "section4Title": "Πνευματική Ιδιοκτησία",
    "section4Detail": "Όλα τα σχέδια, γραφικά, ονόματα προϊόντων και brand assets είναι αποκλειστική ιδιοκτησία της {brand} και δεν επιτρέπεται η αναπαραγωγή ή διανομή χωρίς γραπτή άδεια. Το λογισμικό και η δομή του ιστότοπου προστατεύονται επίσης από πνευματικά δικαιώματα.",
    "section5Title": "Περιορισμός Ευθύνης",
    "section5Detail": "Η συνολική ευθύνη της {brand} για αξιώσεις που απορρέουν από αγορά περιορίζεται στην αξία της συγκεκριμένης παραγγελίας. Δεν ευθυνόμαστε για έμμεσες ή παρεπόμενες ζημίες και δεν εγγυόμαστε αδιάλειπτη πρόσβαση στον ιστότοπο.",
    "section6Title": "Αλλαγές στους Όρους",
    "section6Detail": "Μπορούμε να ενημερώνουμε τους παρόντες όρους ανά πάσα στιγμή. Η ισχύουσα έκδοση κατά την παραγγελία εφαρμόζεται σε αυτήν. Η συνέχιση χρήσης του ιστότοπου μετά από ενημέρωση συνιστά αποδοχή των νέων όρων. Τελευταία ενημέρωση: Μάιος 2026."
  },
  "Privacy": {
    "eyebrow": "Νομικά",
    "heading": "ΠΟΛΙΤΙΚΗ\nΑΠΟΡΡΗΤΟΥ"
  },
  "SizeGuide": {
    "heading": "ΟΔΗΓΟΣ ΜΕΓΕΘΩΝ",
    "eyebrow": "Εφαρμογή",
    "intro": "Όλα τα ρούχα NORELIA. μετρώνται με μεγέθη XS έως XL. Μέτρησε τον εαυτό σου και χρησιμοποίησε τον παρακάτω πίνακα για να βρεις την κατάλληλη εφαρμογή.",
    "measureTitle": "Πώς να μετρηθείς",
    "measureChest": "Στήθος: Μέτρησε οριζόντια γύρω από το πλατύτερο σημείο.",
    "measureWaist": "Μέση: Μέτρησε γύρω από το πιο λεπτό σημείο του κορμού.",
    "measureHip": "Ισχία: Μέτρησε γύρω από το πλατύτερο σημείο.",
    "menTitle": "Ανδρικά",
    "womenTitle": "Γυναικεία",
    "tshirtsTab": "T-SHIRTS",
    "hoodiesTab": "ΦΟΥΤΕΡ",
    "zippersTab": "ΦΕΡΜΟΥΑΡ",
    "studioLink": "Σχεδίασε το δικό σου →"
  },
  "GenderPage": {
    "men": "ΑΝΔΡΑΣ",
    "women": "ΓΥΝΑΙΚΑ",
    "styles": "στυλ"
  }
}
```

- [ ] **Step 2: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add messages/el.json && git commit -m "feat(i18n): add Greek message file with all UI strings"
```

---

## Task 6: TypeScript message types + test-utils intl-wrapper

**Files:**
- Create: `src/types/next-intl.d.ts`
- Create: `src/test-utils/intl-wrapper.tsx`

- [ ] **Step 1: Create src/types/next-intl.d.ts**

```ts
// Provides full TypeScript autocomplete for useTranslations() calls.
// next-intl reads this type to validate key names at compile time.
type Messages = typeof import('../../messages/en.json')

declare interface IntlMessages extends Messages {}
```

- [ ] **Step 2: Create src/test-utils/intl-wrapper.tsx**

```tsx
import React from 'react'
import { NextIntlClientProvider } from 'next-intl'
import enMessages from '../../messages/en.json'

interface Props {
  children: React.ReactNode
  locale?: string
}

/**
 * Wraps a component under test with NextIntlClientProvider using English messages.
 * Use this in any test that renders a component calling useTranslations().
 *
 * Usage:
 *   import { render } from '@testing-library/react'
 *   import { IntlWrapper } from '@/test-utils/intl-wrapper'
 *   render(<IntlWrapper><MyComponent /></IntlWrapper>)
 */
export function IntlWrapper({ children, locale = 'en' }: Props) {
  return (
    <NextIntlClientProvider locale={locale} messages={enMessages}>
      {children}
    </NextIntlClientProvider>
  )
}
```

- [ ] **Step 3: Run TypeScript**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/types/next-intl.d.ts src/test-utils/intl-wrapper.tsx && git commit -m "feat(i18n): add TS message types and test IntlWrapper utility"
```

---

## Task 7: Update globals.css and root layout (fonts + lang switching)

**Files:**
- Modify: `src/app/globals.css`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Add font-switching CSS to globals.css**

In `src/app/globals.css`, find the line:
```css
  /* Font variable bridges */
  --font-display-var: Impact, sans-serif;
  --font-body-var: "Helvetica Neue", sans-serif;
```

Replace with:
```css
  /* Font variable bridges — Latin default */
  --font-display-var: var(--font-display-latin, Impact, sans-serif);
  --font-body-var: "Helvetica Neue", sans-serif;
```

Then find the closing `}` of `@layer base {` block (after the `.dark` block) and add before the `* {` block:

```css
  /* Switch display font to Oswald for Greek locale */
  html[lang="el"] {
    --font-display-var: var(--font-display-greek, Impact, sans-serif);
  }

  html[lang="en"] {
    --font-display-var: var(--font-display-latin, Impact, sans-serif);
  }
```

- [ ] **Step 2: Update src/app/layout.tsx to add Oswald, getLocale, and dual font variables**

Replace the entire file:

```tsx
import type { Metadata } from 'next'
import { Bebas_Neue, Oswald, DM_Sans } from 'next/font/google'
import { getLocale } from 'next-intl/server'
import './globals.css'
import { BRAND } from '@/lib/constants'

const bebasNeue = Bebas_Neue({
  weight: '400',
  subsets: ['latin'],
  variable: '--font-display-latin',
  display: 'swap',
})

const oswald = Oswald({
  weight: ['400', '500'],
  subsets: ['latin', 'greek'],
  variable: '--font-display-greek',
  display: 'swap',
})

const dmSans = DM_Sans({
  subsets: ['latin', 'greek'],
  weight: ['300', '400', '500', '700'],
  variable: '--font-body',
  display: 'swap',
})

export const metadata: Metadata = {
  title: { default: BRAND, template: `%s | ${BRAND}` },
  description: 'Minimalist premium streetwear. Custom printing. Free shipping over €60.',
  metadataBase: new URL('https://norelia.com'),
}

export default async function RootLayout({ children }: { children: React.ReactNode }) {
  const locale = await getLocale()
  return (
    <html
      lang={locale}
      className={`${bebasNeue.variable} ${oswald.variable} ${dmSans.variable}`}
    >
      <body className="font-body bg-surface text-on-surface antialiased">
        {children}
      </body>
    </html>
  )
}
```

- [ ] **Step 3: Run TypeScript**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/app/globals.css src/app/layout.tsx && git commit -m "feat(i18n): add Oswald font for Greek locale, CSS variable switching"
```

---

## Task 8: Create src/app/[locale]/layout.tsx and migrate [locale] shell

**Files:**
- Create: `src/app/[locale]/layout.tsx`

This layout wraps all localized routes with `NextIntlClientProvider` and renders Nav, Footer, and overlay components.

- [ ] **Step 1: Create src/app/[locale]/layout.tsx**

```tsx
import { NextIntlClientProvider } from 'next-intl'
import { getMessages, getTranslations } from 'next-intl/server'
import { notFound } from 'next/navigation'
import type { Metadata } from 'next'
import { BRAND } from '@/lib/constants'
import { Nav }           from '@/components/layout/nav'
import { SiteFooter }    from '@/components/layout/site-footer'
import { NewsletterBar } from '@/components/layout/newsletter-bar'
import { SidePanel }     from '@/components/layout/side-panel'
import { Toast }         from '@/components/layout/toast'
import { GDPRBanner }    from '@/components/layout/gdpr-banner'
import { SignInModal }             from '@/components/modals/sign-in-modal'
import { CheckoutComingSoonModal } from '@/components/modals/checkout-coming-soon-modal'
import { ScrollToTop }             from '@/components/scroll-to-top'

const LOCALES = ['el', 'en'] as const

export function generateStaticParams() {
  return LOCALES.map(locale => ({ locale }))
}

interface Props {
  children:  React.ReactNode
  params:    Promise<{ locale: string }>
}

export default async function LocaleLayout({ children, params }: Props) {
  const { locale } = await params
  if (!LOCALES.includes(locale as typeof LOCALES[number])) notFound()

  const messages = await getMessages()

  return (
    <NextIntlClientProvider locale={locale} messages={messages}>
      <Nav />
      <main>{children}</main>
      <NewsletterBar />
      <SiteFooter />
      <SidePanel />
      <Toast />
      <GDPRBanner />
      <SignInModal />
      <CheckoutComingSoonModal />
      <ScrollToTop />
    </NextIntlClientProvider>
  )
}
```

- [ ] **Step 2: Run TypeScript**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add "src/app/[locale]/layout.tsx" && git commit -m "feat(i18n): add [locale] layout with NextIntlClientProvider"
```

---

## Task 9: Migrate all route pages to src/app/[locale]/

Move existing page files. The current `src/app/layout.tsx` no longer renders Nav/Footer (moved to `[locale]/layout.tsx`). Delete the overlapping components from the root layout (already done in Task 7).

- [ ] **Step 1: Move home page**

Create `src/app/[locale]/page.tsx` by copying `src/app/page.tsx`. Then delete `src/app/page.tsx`.

The new `src/app/[locale]/page.tsx` needs `getTranslations` for FeaturedCarousel titles. Replace the entire file:

```tsx
import type { Metadata } from 'next'
import { getTranslations } from 'next-intl/server'
import { Hero }              from '@/components/home/hero'
import { Marquee }           from '@/components/home/marquee'
import { FeaturedCarousel }  from '@/components/home/featured-carousel'
import { PRODUCTS }          from '@/data/products'

export const metadata: Metadata = {
  title: 'NORELIA. Premium Streetwear',
  description: 'Minimal premium streetwear. New collections, trending styles, and exclusive drops.',
}

export default async function HomePage() {
  const t = await getTranslations('Home')
  const newProducts      = PRODUCTS.filter((p) => p.tag === 'NEW')
  const trendingProducts = PRODUCTS.slice(0, 8)
  const saleProducts     = PRODUCTS.filter((p) => p.salePrice !== undefined)

  return (
    <>
      <Hero />
      <Marquee dark={true} />
      <FeaturedCarousel
        title={t('newInTitle')}
        subtitle={t('newInSubtitle')}
        products={newProducts}
        viewAllHref="/men?filter=NEWIN"
      />
      <FeaturedCarousel
        title={t('trendingTitle')}
        subtitle={t('trendingSubtitle')}
        products={trendingProducts}
        viewAllHref="/men"
      />
      <FeaturedCarousel
        title={t('onSaleTitle')}
        subtitle={t('onSaleSubtitle')}
        products={saleProducts}
        viewAllHref="/men?filter=SALES"
      />
    </>
  )
}
```

- [ ] **Step 2: Move [gender] page**

Create `src/app/[locale]/[gender]/page.tsx` from `src/app/[gender]/page.tsx`. Update params type to include locale and add `getTranslations`:

```tsx
import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { getTranslations } from 'next-intl/server'
import { ProductGrid } from '@/components/products/product-grid'
import { byGender, CAT_SLUG_TO_FILTER } from '@/lib/utils'

interface Props {
  params:       Promise<{ locale: string; gender: string }>
  searchParams: Promise<{ filter?: string }>
}

const VALID_GENDERS   = ['men', 'women']
const VALID_CAT_FILTERS = new Set(Object.values(CAT_SLUG_TO_FILTER))

export function generateStaticParams() {
  return [
    { locale: 'el', gender: 'men' },
    { locale: 'el', gender: 'women' },
    { locale: 'en', gender: 'men' },
    { locale: 'en', gender: 'women' },
  ]
}

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { gender } = await params
  if (!VALID_GENDERS.includes(gender)) return {}
  const label = gender === 'men' ? 'Men' : 'Women'
  return {
    title: label,
    description: `Shop the full ${label.toLowerCase()} collection.`,
  }
}

export default async function GenderPage({ params, searchParams }: Props) {
  const { gender } = await params
  const { filter: rawFilter } = await searchParams
  const t = await getTranslations('GenderPage')

  if (!VALID_GENDERS.includes(gender)) notFound()

  const g = gender as 'men' | 'women'
  const pool = byGender(g)
  let activeFilter: string | undefined
  let products = pool

  if (rawFilter) {
    if (rawFilter === 'NEWIN') {
      activeFilter = 'NEWIN'
      products = pool.filter(p => p.tag === 'NEW')
    } else if (rawFilter === 'SALES') {
      activeFilter = 'SALES'
      products = pool.filter(p => p.salePrice != null)
    } else if (VALID_CAT_FILTERS.has(rawFilter)) {
      activeFilter = rawFilter
      products = pool.filter(p => p.cat === rawFilter)
    }
  }

  return (
    <div className="min-h-screen pt-20 bg-surface">
      <div className="max-w-[1440px] mx-auto px-4 md:px-[60px] py-12">
        <ProductGrid
          products={products}
          title={gender === 'men' ? t('men') : t('women')}
          subtitle={`${products.length} ${t('styles')}`}
          activeFilter={activeFilter}
          filterBase={`/${gender}`}
          genderContext={g}
        />
      </div>
    </div>
  )
}
```

Then delete `src/app/[gender]/page.tsx` and `src/app/[gender]/page.test.tsx` (tests will be re-added later).

- [ ] **Step 3: Move [gender]/[category] page**

Create `src/app/[locale]/[gender]/[category]/page.tsx` from the existing file. Add `locale` to params type. Delete original `src/app/[gender]/[category]/page.tsx`.

The updated params interface:
```tsx
interface Props {
  params: Promise<{ locale: string; gender: string; category: string }>
}
```

`generateStaticParams` returns all locale/gender/category combinations.

- [ ] **Step 4: Move product, checkout, studio, our-studio, size-guide pages**

Copy each to `src/app/[locale]/[route]/page.tsx`. Update params to include `locale`. Delete originals.

For `product/[id]/page.tsx` params:
```tsx
interface Props {
  params:       Promise<{ locale: string; id: string }>
  searchParams: Promise<{ color?: string; from?: string }>
}
```

- [ ] **Step 5: Move info pages (about, shipping, returns, terms, privacy)**

Copy each to `src/app/[locale]/[route]/page.tsx`. Delete originals.

- [ ] **Step 6: Move error.tsx, not-found.tsx, loading.tsx**

Copy `src/app/error.tsx` → `src/app/[locale]/error.tsx`  
Copy `src/app/not-found.tsx` → `src/app/[locale]/not-found.tsx`  
Copy `src/app/loading.tsx` → `src/app/[locale]/loading.tsx`  
Delete originals.

- [ ] **Step 7: Run build to check routing**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm run build 2>&1 | tail -30
```

Expected: builds without routing errors. TypeScript errors about missing translations expected — resolved in later tasks.

- [ ] **Step 8: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add "src/app/[locale]/" && git rm -r src/app/page.tsx "src/app/[gender]/" src/app/product/ src/app/checkout/ src/app/studio/ src/app/our-studio/ src/app/size-guide/ src/app/about/ src/app/shipping/ src/app/returns/ src/app/terms/ src/app/privacy/ src/app/not-found.tsx src/app/error.tsx src/app/loading.tsx 2>/dev/null || true && git add -A && git commit -m "feat(i18n): migrate all routes to [locale] segment"
```

---

## Task 10: Create Flag SVG components

**Files:**
- Create: `src/components/icons/flag-greece.tsx`
- Create: `src/components/icons/flag-uk.tsx`

- [ ] **Step 1: Create src/components/icons/flag-greece.tsx**

```tsx
interface Props { className?: string }

export function FlagGreece({ className }: Props) {
  const stripeH = 14 / 9
  return (
    <svg viewBox="0 0 20 14" className={className} aria-hidden="true" focusable="false">
      {/* 9 horizontal stripes, alternating blue / white */}
      {Array.from({ length: 9 }).map((_, i) => (
        <rect
          key={i}
          x="0"
          y={i * stripeH}
          width="20"
          height={stripeH + 0.1} /* +0.1 closes sub-pixel gaps */
          fill={i % 2 === 0 ? '#0D5EAF' : '#FFFFFF'}
        />
      ))}
      {/* Blue canton: 8/20 wide, 5/9 tall */}
      <rect x="0" y="0" width="8" height={14 * 5 / 9} fill="#0D5EAF" />
      {/* White cross — vertical bar */}
      <rect x="3.2" y="0" width="1.6" height={14 * 5 / 9} fill="#FFFFFF" />
      {/* White cross — horizontal bar (centred in canton height) */}
      <rect
        x="0"
        y={14 * 5 / 9 / 2 - 0.8}
        width="8"
        height="1.6"
        fill="#FFFFFF"
      />
    </svg>
  )
}
```

- [ ] **Step 2: Create src/components/icons/flag-uk.tsx**

```tsx
interface Props { className?: string }

export function FlagUK({ className }: Props) {
  return (
    <svg viewBox="0 0 20 14" className={className} aria-hidden="true" focusable="false">
      {/* Blue field */}
      <rect width="20" height="14" fill="#012169" />
      {/* White diagonal cross (St Andrew + St Patrick combined — simplified) */}
      <line x1="0" y1="0" x2="20" y2="14" stroke="#FFFFFF" strokeWidth="3.5" />
      <line x1="20" y1="0" x2="0" y2="14" stroke="#FFFFFF" strokeWidth="3.5" />
      {/* Red diagonals (St Patrick) */}
      <line x1="0" y1="0" x2="20" y2="14" stroke="#C8102E" strokeWidth="1.8" />
      <line x1="20" y1="0" x2="0" y2="14" stroke="#C8102E" strokeWidth="1.8" />
      {/* White cross of St George */}
      <rect x="8" y="0" width="4" height="14" fill="#FFFFFF" />
      <rect x="0" y="5" width="20" height="4" fill="#FFFFFF" />
      {/* Red cross of St George */}
      <rect x="8.8" y="0" width="2.4" height="14" fill="#C8102E" />
      <rect x="0" y="5.8" width="20" height="2.4" fill="#C8102E" />
    </svg>
  )
}
```

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/components/icons/ && git commit -m "feat(i18n): add Greek and UK flag SVG components"
```

---

## Task 11: Create LanguageSwitcher component and tests

**Files:**
- Create: `src/components/layout/language-switcher.tsx`
- Create: `src/components/layout/language-switcher.test.tsx`

- [ ] **Step 1: Write the failing test first**

Create `src/components/layout/language-switcher.test.tsx`:

```tsx
import React from 'react'
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { IntlWrapper } from '@/test-utils/intl-wrapper'

vi.mock('@/navigation', () => ({
  useRouter:   () => ({ replace: vi.fn() }),
  usePathname: () => '/',
}))
vi.mock('next-intl', () => ({
  useLocale: () => 'en',
}))

import { LanguageSwitcher } from './language-switcher'

describe('LanguageSwitcher', () => {
  it('renders EL and EN buttons', () => {
    render(<IntlWrapper><LanguageSwitcher /></IntlWrapper>)
    expect(screen.getByLabelText(/switch to greek/i)).toBeTruthy()
    expect(screen.getByLabelText(/switch to english/i)).toBeTruthy()
  })

  it('active locale (en) has opacity-100 class', () => {
    render(<IntlWrapper><LanguageSwitcher /></IntlWrapper>)
    const enBtn = screen.getByLabelText(/switch to english/i)
    expect(enBtn.className).toContain('opacity-100')
  })

  it('inactive locale (el) has opacity-45 class', () => {
    render(<IntlWrapper><LanguageSwitcher /></IntlWrapper>)
    const elBtn = screen.getByLabelText(/switch to greek/i)
    expect(elBtn.className).toContain('opacity-45')
  })

  it('calls router.replace when inactive locale clicked', () => {
    const replace = vi.fn()
    vi.mock('@/navigation', () => ({
      useRouter:   () => ({ replace }),
      usePathname: () => '/',
    }))
    render(<IntlWrapper><LanguageSwitcher /></IntlWrapper>)
    fireEvent.click(screen.getByLabelText(/switch to greek/i))
    // router.replace called with pathname and locale
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test -- language-switcher 2>&1 | tail -15
```

Expected: FAIL — `LanguageSwitcher` not found.

- [ ] **Step 3: Create src/components/layout/language-switcher.tsx**

```tsx
'use client'

import { useLocale } from 'next-intl'
import { useRouter, usePathname } from '@/navigation'
import { FlagGreece } from '@/components/icons/flag-greece'
import { FlagUK }     from '@/components/icons/flag-uk'
import { cn }         from '@/lib/utils'

export function LanguageSwitcher() {
  const locale   = useLocale()
  const router   = useRouter()
  const pathname = usePathname()

  function switchLocale(next: 'el' | 'en') {
    if (next === locale) return
    router.replace(pathname, { locale: next })
  }

  return (
    <div className="flex items-center gap-2.5">
      <button
        onClick={() => switchLocale('el')}
        aria-label="Switch to Greek"
        className={cn(
          'flex items-center gap-1.5 font-body text-[10px] tracking-[0.12em] uppercase transition-opacity duration-150',
          locale === 'el' ? 'opacity-100' : 'opacity-45 hover:opacity-70',
        )}
      >
        <FlagGreece className="w-5 h-3.5 rounded-[1px]" />
        EL
      </button>
      <button
        onClick={() => switchLocale('en')}
        aria-label="Switch to English"
        className={cn(
          'flex items-center gap-1.5 font-body text-[10px] tracking-[0.12em] uppercase transition-opacity duration-150',
          locale === 'en' ? 'opacity-100' : 'opacity-45 hover:opacity-70',
        )}
      >
        <FlagUK className="w-5 h-3.5 rounded-[1px]" />
        EN
      </button>
    </div>
  )
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test -- language-switcher 2>&1 | tail -15
```

Expected: PASS all 4 tests.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/components/layout/language-switcher.tsx src/components/layout/language-switcher.test.tsx && git commit -m "feat(i18n): add LanguageSwitcher with flag SVGs and tests"
```

---

## Task 12: Translate Nav

**Files:**
- Modify: `src/components/layout/nav.tsx`
- Modify: `src/components/layout/nav.test.tsx`

- [ ] **Step 1: Update nav.test.tsx to use IntlWrapper and mock next-intl + @/navigation**

Add at top of `nav.test.tsx` (after existing vi.mock lines):

```tsx
vi.mock('next-intl', () => ({ useTranslations: () => (key: string) => key }))
vi.mock('@/navigation', () => ({
  usePathname: () => '/',
}))
```

Change the existing `vi.mock('next/navigation', ...)` mock to also include the `Link` export, OR keep the `next/navigation` mock and add the `@/navigation` mock separately. The nav component will import `Link` from `@/navigation` after this task.

Also wrap each `render(<Nav />)` with `<IntlWrapper>`:

```tsx
import { IntlWrapper } from '@/test-utils/intl-wrapper'
// ...
render(<IntlWrapper><Nav /></IntlWrapper>)
```

- [ ] **Step 2: Run test to see current state**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test -- nav.test 2>&1 | tail -20
```

- [ ] **Step 3: Update nav.tsx**

Replace the two imports at the top:

```tsx
import Link from 'next/link'
// ...
import { usePathname } from 'next/navigation'
```

With:

```tsx
import { Link, usePathname } from '@/navigation'
```

Add `useTranslations` import:

```tsx
import { useTranslations } from 'next-intl'
```

Add `LanguageSwitcher` import:

```tsx
import { LanguageSwitcher } from '@/components/layout/language-switcher'
```

Inside `Nav()` function, add at the top of the function body:

```tsx
const t = useTranslations('Nav')
```

Replace all hardcoded nav strings:
- `"Home"` → `{t('home')}`
- `"Men"` (link label) → `{t('men')}`
- `"Women"` (link label) → `{t('women')}`
- `"Design Your Own"` → `{t('designYourOwn')}`
- `"Sign In"` (button text) → `{t('signIn')}`
- `aria-label="Sign in to your account"` → `aria-label={t('signInLabel')}`
- `aria-label="Search products"` → `aria-label={t('searchLabel')}`
- `placeholder="Search products..."` → `placeholder={t('searchPlaceholder')}`
- `"No results for"` → `{t('noResults')}`
- `aria-label={`Favorites${...}`}` → `aria-label={totalFavCount > 0 ? t('favoritesWithCount', { count: totalFavCount }) : t('favoritesLabel')}`
- `aria-label={`Cart${...}`}` → `aria-label={totalCartCount > 0 ? t('cartWithCount', { count: totalCartCount }) : t('cartLabel')}`
- `aria-label="Menu"` → `aria-label={t('menuLabel')}`
- `aria-label="Close"` in search button → kept as-is (icon button)

In the desktop nav, add `LanguageSwitcher` after the Sign In button:

```tsx
{/* Language switcher */}
<div className="w-px h-3.5 bg-on-surface/15" />
<LanguageSwitcher />
```

In the mobile menu, add language switcher after the Sign In button:

```tsx
<div className="mt-5 flex items-center gap-3">
  <LanguageSwitcher />
</div>
```

Remove `uppercase` CSS class from the nav link text spans that render translated text (since Greek pre-uppercase strings must not be CSS-uppercased). The underline spans and tracking classes remain. Specifically, remove `uppercase` from: Home link, Men link, Women link, Design Your Own link — keep `uppercase` on dropdown category items (they stay English).

- [ ] **Step 4: Run all tests**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test -- nav.test 2>&1 | tail -20
```

Expected: all pass.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/components/layout/nav.tsx src/components/layout/nav.test.tsx && git commit -m "feat(i18n): translate Nav and add LanguageSwitcher"
```

---

## Task 13: Translate Hero, FeaturedCarousel, ProductCard, ProductPage

**Files:**
- Modify: `src/components/home/hero.tsx`
- Modify: `src/components/home/featured-carousel.tsx`
- Modify: `src/components/products/product-card.tsx`
- Modify: `src/components/products/product-page.tsx`
- Modify: `src/components/modals/size-mini-guide.tsx`

- [ ] **Step 1: Translate Hero**

Add to `hero.tsx`:
```tsx
import { useTranslations } from 'next-intl'
```

Inside `Hero()`:
```tsx
const t = useTranslations('Hero')
```

Replace:
```tsx
<h1 className="sr-only">NORELIA. Premium Streetwear</h1>
<HeroHalf side="women" label="WOMEN" ... womenAriaLabel ...
<HeroHalf side="men"   label="MEN"   ...
```

With:
```tsx
<h1 className="sr-only">{t('srHeading')}</h1>
<HeroHalf side="women" label={t('womenLabel')} shopLabel={t('shopEyebrow')} ariaLabel={t('womenAriaLabel')} ...
<HeroHalf side="men"   label={t('menLabel')}   shopLabel={t('shopEyebrow')} ariaLabel={t('menAriaLabel')}   ...
```

Update `HeroHalfProps` to include `shopLabel: string` and `ariaLabel: string`. Replace hardcoded `"SHOP"` with `{shopLabel}` and `aria-label={`Shop ${label}`}` with `aria-label={ariaLabel}`.

- [ ] **Step 2: Translate FeaturedCarousel**

Add:
```tsx
import { useTranslations } from 'next-intl'
```

Inside `FeaturedCarousel()`:
```tsx
const t = useTranslations('FeaturedCarousel')
```

Replace:
```tsx
<span className="font-body text-[11px] tracking-widest uppercase text-on-surface">
  View All
</span>
```

With:
```tsx
<span className="font-body text-[11px] tracking-widest uppercase text-on-surface">
  {t('viewAll')}
</span>
```

Replace:
```tsx
aria-label={`Go to page ${idx + 1}`}
```

With:
```tsx
aria-label={t('goToPage', { n: idx + 1 })}
```

- [ ] **Step 3: Translate ProductCard**

Add:
```tsx
import { useTranslations } from 'next-intl'
```

Update `next/navigation` imports to use `@/navigation`:
```tsx
import { useRouter, usePathname } from '@/navigation'
```

Also update `Link` import:
```tsx
import { Link } from '@/navigation'
```

Inside `ProductCard()`:
```tsx
const t = useTranslations('ProductCard')
```

Replace hardcoded strings:
- `aria-label={favorited ? 'Remove from favorites' : 'Add to favorites'}` → `aria-label={favorited ? t('removeFromFavorites') : t('addToFavorites')}`
- `outOfStock ? 'OUT OF STOCK' : 'QUICK ADD'` → `outOfStock ? t('outOfStock') : t('quickAdd')`
- `product.tag === 'NEW' && <span>NEW</span>` → `product.tag === 'NEW' && <span>{t('new')}</span>`
- `showToast(favorited ? 'Removed from favorites' : 'Added to favorites', ...)` → `showToast(favorited ? t('removedFromFavorites') : t('addedToFavorites'), favorited ? 'remove' : 'add', 'fav')`
- `showToast(`No more ${size} left for ${product.name}`, 'remove')` → `showToast(t('noMoreStock', { size, name: product.name }), 'remove')`
- `showToast(`${product.name}, ${size} added`, 'add')` → `showToast(t('sizeAdded', { name: product.name, size }), 'add', 'cart')`

- [ ] **Step 4: Translate ProductPage**

Add:
```tsx
import { useTranslations } from 'next-intl'
import { useRouter } from '@/navigation'
import { Link } from '@/navigation'
```

Remove `import { useRouter } from 'next/navigation'` and `import Link from 'next/link'`.

Inside `ProductPage()`:
```tsx
const t = useTranslations('ProductPage')
```

Replace all hardcoded strings with `t('key')` calls. Key mappings:
- `"Size"` → `t('sizeLabel')`
- `"Add to Cart"` → `t('addToCart')`
- `"Add to Favourites"` / `"Remove from Favourites"` → `t('addToFavorites')` / `t('removeFromFavorites')`
- `"Sold Out"` → `t('soldOut')`
- `"Size Guide"` → `t('sizeGuide')`
- `"Free"` → `t('free')`
- `"measurements in cm"` → `t('measurementsCm')`
- Toast calls: `showToast(`${product.name} added to cart`, 'add')` → `showToast(t('addedToCart', { name: product.name }), 'add', 'cart')`
- Favourite toasts similarly with `'fav'` kind

- [ ] **Step 5: Translate SizeMiniGuide**

Add:
```tsx
import { useTranslations } from 'next-intl'
```

Inside `SizeMiniGuide()`:
```tsx
const t = useTranslations('ProductPage')
```

Replace:
```tsx
<DialogTitle className="font-display text-2xl tracking-[0.12em]">
  SIZE GUIDE
</DialogTitle>
<p className="font-body text-[10px] text-on-surface/50 mb-4">
  {product.name} · measurements in cm
</p>
```

With:
```tsx
<DialogTitle className="font-display text-2xl tracking-[0.12em]">
  {t('sizeGuide')}
</DialogTitle>
<p className="font-body text-[10px] text-on-surface/50 mb-4">
  {product.name} · {t('measurementsCm')}
</p>
```

- [ ] **Step 6: Update test files (hero, featured-carousel, product-card, product-page)**

For each test file that renders these components, add:
```tsx
vi.mock('next-intl', () => ({ useTranslations: () => (key: string) => key }))
vi.mock('@/navigation', () => ({
  useRouter:   () => ({ push: vi.fn(), replace: vi.fn() }),
  usePathname: () => '/',
  Link: ({ href, children, ...rest }: { href: string; children: React.ReactNode; [key: string]: unknown }) =>
    <a href={href} {...(rest as React.AnchorHTMLAttributes<HTMLAnchorElement>)}>{children}</a>,
}))
```

And wrap renders with `<IntlWrapper>`:
```tsx
import { IntlWrapper } from '@/test-utils/intl-wrapper'
render(<IntlWrapper><ProductCard product={mockProduct} /></IntlWrapper>)
```

- [ ] **Step 7: Run all tests**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test 2>&1 | tail -30
```

- [ ] **Step 8: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/components/home/ src/components/products/ src/components/modals/size-mini-guide.tsx && git commit -m "feat(i18n): translate Hero, FeaturedCarousel, ProductCard, ProductPage, SizeMiniGuide"
```

---

## Task 14: Translate SidePanel, Toast, NewsletterBar, GDPRBanner, SiteFooter

**Files:**
- Modify: `src/components/layout/side-panel.tsx`
- Modify: `src/components/layout/toast.tsx`
- Modify: `src/components/layout/newsletter-bar.tsx`
- Modify: `src/components/layout/gdpr-banner.tsx`
- Modify: `src/components/layout/site-footer.tsx`

- [ ] **Step 1: Update Toast to use kind field instead of string-sniffing**

In `src/components/layout/toast.tsx`, replace the `isFav` detection:

```tsx
// Old — string-sniffing, breaks with translated messages:
const isFav =
  toast.type === 'add' &&
  (toast.msg.toLowerCase().includes('saved') ||
    toast.msg.toLowerCase().includes('favourites') ||
    toast.msg.toLowerCase().includes('favorites'))
```

With:

```tsx
// New — uses the kind field set at the call site:
const isFav = toast.type === 'add' && toast.kind === 'fav'
```

No other changes to Toast needed — it renders `toast.msg` directly.

- [ ] **Step 2: Translate SidePanel**

Add:
```tsx
import { useTranslations } from 'next-intl'
import { useRouter } from '@/navigation'
```

Remove `import { useRouter } from 'next/navigation'`.

Inside `SidePanel()`:
```tsx
const t = useTranslations('SidePanel')
```

Replace hardcoded strings:
- `'MY CART'` → `{t('cart')}`
- `'FAVORITES'` → `{t('favorites')}`
- `aria-label="Close panel"` → `aria-label={t('close')}`
- `'Your cart is empty.'` → `{t('cartEmpty')}`
- `'No saved items yet.'` → `{t('noSaved')}`
- `aria-label={isFav ? 'Remove from saved' : 'Save for later'}` → `aria-label={isFav ? t('removeFromSaved') : t('saveForLater')}`
- `aria-label="Remove item"` → `aria-label={t('removeItem')}`
- `'Order Total'` → `{t('orderTotal')}`
- `'Free shipping on your order!'` → `{t('freeShipping')}`
- `\`Add €${...} more for free shipping\`` → `{t('addMoreForFreeShipping', { amount: (60 - total).toFixed(2) })}`
- `'Proceed to Checkout'` → `{t('checkout')}`
- Toast calls: update to use `t('removedFromCart', { name: item.name })` with `kind: 'cart'`, `t('addedToFavorites', ...)` with `kind: 'fav'`, etc.

Also in `FavItemRow`, translate:
- `\`Remove ${product.name} from saved\`` → `t('removeFromSaved')` (aria-label for the remove button)
- `'Remove'` text label → `t('removeItem')` ... or keep as is since it's a small text label inside the button

- [ ] **Step 3: Translate NewsletterBar**

Add:
```tsx
import { useTranslations } from 'next-intl'
```

Inside `NewsletterBar()`:
```tsx
const t = useTranslations('NewsletterBar')
```

Replace:
- `'Stay in the loop.'` → `{t('headline')}`
- `'Early access to drops & exclusive offers'` → `{t('subtitle')}`
- `placeholder="your@email.com"` → `placeholder={t('placeholder')}`
- `'Subscribe'` button text → `{t('subscribe')}`
- `"✓ You're in."` → `{t('success')}`

Remove `uppercase` CSS class from the subtitle `<p>` (Greek subtitle is pre-uppercased in el.json).

- [ ] **Step 4: Translate GDPRBanner**

Add:
```tsx
import { useTranslations } from 'next-intl'
```

Inside `GDPRBanner()`:
```tsx
const t = useTranslations('GDPRBanner')
```

Replace:
- `'We use cookies'` → `{t('title')}`
- Body text paragraph → `{t('body')}`
- `aria-label="Decline non-essential cookies"` → `aria-label={t('declineLabel')}`
- `'Decline'` button text → `{t('decline')}`
- `aria-label="Accept all cookies"` → `aria-label={t('acceptLabel')}`
- `'Accept All'` button text → `{t('acceptAll')}`

- [ ] **Step 5: Translate SiteFooter**

`SiteFooter` is a server component (no `'use client'`). Convert to async and use `getTranslations`:

```tsx
import { getTranslations } from 'next-intl/server'
```

Convert internal sub-functions to accept `t` as a prop or inline the translation calls. Simplest approach: make `SiteFooter` async and pass `t` down:

```tsx
export async function SiteFooter() {
  const t = await getTranslations('SiteFooter')
  return (
    <footer className="dark bg-surface-alt text-on-surface">
      {/* Main columns */}
      <div className="max-w-[1440px] mx-auto px-6 md:px-[60px] py-16">
        <div className="grid grid-cols-1 gap-12 sm:grid-cols-2 lg:grid-cols-4">
          <BrandColumn t={t} />
          <InfoColumn t={t} />
          <ContactColumn t={t} />
          <PaymentColumn t={t} />
        </div>
      </div>
      <div className="border-t border-border">
        <div className="max-w-[1440px] mx-auto px-6 md:px-[60px] py-5 grid grid-cols-1 sm:grid-cols-3 items-center gap-y-2 text-center sm:text-left">
          <p className="font-body text-[10px] tracking-[0.15em] text-on-surface/35">
            {t('copyright', { year: new Date().getFullYear() })}
          </p>
          <p className="font-body text-[10px] tracking-[0.15em] text-on-surface/55 text-center">
            {t('tagline')}
          </p>
          <p className="font-body text-[10px] tracking-[0.12em] text-on-surface/35 sm:text-right">
            {t('secure')}
          </p>
        </div>
      </div>
    </footer>
  )
}
```

Update each sub-component to accept `t: (key: string, values?: Record<string, unknown>) => string` and replace hardcoded strings.

Remove `uppercase` CSS class from column headings (pre-uppercased in JSON where needed). Keep `uppercase` on the bottom strip text which is already using token values.

- [ ] **Step 6: Update affected test files**

For `site-footer.test.tsx`, `newsletter-bar.test.tsx`, `gdpr-banner.test.tsx`, `side-panel.test.tsx`, `toast.test.tsx`:

- Add `vi.mock('next-intl', ...)` where components use client hooks
- For `SiteFooter` (server component): add `vi.mock('next-intl/server', () => ({ getTranslations: async () => (key: string) => key }))`
- Wrap renders in `<IntlWrapper>` where needed

Update GDPR test assertion since title changed:
- `screen.getByText('We use cookies')` now resolves from `t('title')` which returns `'title'` in the mock — update test to match mocked key: `screen.getByText('title')` OR use the real en.json value by providing real messages in the wrapper

Simplest: keep using `IntlWrapper` (loads real en.json), so existing text assertions still work.

- [ ] **Step 7: Run all tests**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test 2>&1 | tail -30
```

Expected: all tests pass.

- [ ] **Step 8: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/components/layout/ && git commit -m "feat(i18n): translate SidePanel, Toast, NewsletterBar, GDPRBanner, SiteFooter"
```

---

## Task 15: Translate modals, BackButton, ScrollToTop, and remaining components

**Files:**
- Modify: `src/components/modals/sign-in-modal.tsx`
- Modify: `src/components/modals/checkout-coming-soon-modal.tsx`
- Modify: `src/components/layout/back-button.tsx`
- Modify: `src/components/scroll-to-top.tsx`

- [ ] **Step 1: Translate SignInModal**

Add `useTranslations('SignInModal')` and replace:
- `'SIGN IN'` / `'CREATE ACCOUNT'` title → `{tab === 'signin' ? t('signInTitle') : t('registerTitle')}`
- Tab labels: `'Sign In'` → `{t('signInTab')}`, `'Create Account'` → `{t('registerTab')}`
- `placeholder="Email"` → `placeholder={t('emailPlaceholder')}`
- `placeholder="Password"` → `placeholder={t('passwordPlaceholder')}`
- `placeholder="Full Name"` → `placeholder={t('namePlaceholder')}`
- `'Forgot your password?'` → `{t('forgotPassword')}`
- Submit button text → `{tab === 'signin' ? t('signInButton') : t('createButton')}`
- `aria-label="Close"` → `aria-label={t('close')}`
- `'UI only · authentication coming soon'` → `{t('comingSoon')}`

- [ ] **Step 2: Translate CheckoutComingSoonModal**

Add `useTranslations('CheckoutComingSoonModal')` and replace:
- `'CHECKOUT'` → `{t('title')}`
- Body text → `{t('body')}`
- `'Close'` button → `{t('close')}`

- [ ] **Step 3: Translate BackButton**

Add `useTranslations('BackButton')` and replace `'← Back'` → `{t('label')}`.

Also update `useRouter` import:
```tsx
import { useRouter } from '@/navigation'
```

- [ ] **Step 4: Translate ScrollToTop**

Add `useTranslations('ScrollToTop')` and replace:
- `aria-label="Scroll to top"` → `aria-label={t('label')}`
- `'TOP'` label span → `{t('top')}`

- [ ] **Step 5: Update test files for these components**

For each test: add `vi.mock('next-intl', () => ({ useTranslations: () => (key: string) => key }))` and `vi.mock('@/navigation', ...)`. Wrap renders in `<IntlWrapper>`.

For `sign-in-modal.test.tsx` assertions, update string matches to use the key name (when mocking) or wrap with `IntlWrapper` and use real English strings.

- [ ] **Step 6: Run all tests**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test 2>&1 | tail -30
```

- [ ] **Step 7: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add src/components/modals/ src/components/layout/back-button.tsx src/components/scroll-to-top.tsx && git commit -m "feat(i18n): translate modals, BackButton, and ScrollToTop"
```

---

## Task 16: Translate all info pages

**Files:**
- Modify: `src/app/[locale]/about/page.tsx`
- Modify: `src/app/[locale]/our-studio/page.tsx`
- Modify: `src/app/[locale]/shipping/page.tsx`
- Modify: `src/app/[locale]/returns/page.tsx`
- Modify: `src/app/[locale]/terms/page.tsx`
- Modify: `src/app/[locale]/privacy/page.tsx`
- Modify: `src/app/[locale]/not-found.tsx`
- Modify: `src/app/[locale]/size-guide/page.tsx`
- Modify: `src/app/[locale]/checkout/page.tsx`

All these are server components. Add `getTranslations` to each.

- [ ] **Step 1: Translate About page**

Replace the hardcoded `PILLARS` array and all inline strings with `getTranslations('About')`:

```tsx
export default async function AboutPage() {
  const t = await getTranslations('About')

  return (
    <main className="min-h-screen bg-surface">
      <section className="dark bg-surface-alt pt-32 pb-[72px] px-4 md:px-[60px]">
        <div className="max-w-[1440px] mx-auto">
          <BackButton />
          <p className="font-body text-[10px] tracking-[0.3em] text-on-surface/40 mb-3">
            {t('eyebrow')}
          </p>
          <h1 className="font-display text-7xl md:text-[88px] tracking-[0.04em] text-on-surface leading-[0.95]">
            {t('heading', { brand: BRAND }).split('\n').map((line, i) => (
              <span key={i}>{line}{i === 0 && <br />}</span>
            ))}
          </h1>
        </div>
      </section>
      <section className="max-w-[1440px] mx-auto px-4 md:px-[60px] py-20">
        <div className="max-w-[720px] mb-20">
          <p className="font-display text-[22px] tracking-[0.14em] text-on-surface mb-5">
            {t('introTitle')}
          </p>
          <p className="font-body text-[15px] text-on-surface-muted leading-[1.85] tracking-[0.02em] mb-5">
            {t('intro1', { brand: BRAND })}
          </p>
          <p className="font-body text-[15px] text-on-surface-muted leading-[1.85] tracking-[0.02em] mb-5">
            {t('intro2', { brand: BRAND })}
          </p>
          <p className="font-body text-[15px] text-on-surface-muted leading-[1.85] tracking-[0.02em]">
            {t('intro3')}
          </p>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-[2px] mb-20">
          {(['01', '02', '03'] as const).map((num) => (
            <div key={num} className="bg-surface-raised px-9 py-10">
              <p className="font-display text-[13px] tracking-[0.22em] text-on-surface/30 mb-3.5">{num}</p>
              <p className="font-display text-2xl tracking-[0.1em] text-on-surface mb-4">
                {t(`pillar${num}Title`)}
              </p>
              <p className="font-body text-[13px] text-on-surface-muted leading-[1.8] tracking-[0.02em]">
                {t(`pillar${num}Body`)}
              </p>
            </div>
          ))}
        </div>
        <div className="border-t border-border-subtle pt-12 max-w-[720px]">
          <p className="font-display text-[32px] tracking-[0.08em] text-on-surface mb-4">
            {t('closingTitle')}
          </p>
          <p className="font-body text-sm text-on-surface-muted leading-[1.85] tracking-[0.02em]">
            {t('closingBody', { brand: BRAND })}
          </p>
        </div>
      </section>
    </main>
  )
}
```

- [ ] **Step 2: Translate OurStudio page**

Use `getTranslations('OurStudio')`. Replace all hardcoded strings with `t('key')` calls. Replace the hardcoded `HOURS` array with individual translation keys `t('monFri')`, `t('saturday')`, `t('sunday')`, `t('monFriHours')`, `t('saturdayHours')`, `t('closed')`. Replace address lines with `t('address1')`, `t('address2')`, `t('address3')`.

- [ ] **Step 3: Translate Shipping page**

Use `getTranslations('Shipping')`. Replace `METHODS` array with individual `t('method1Title')`, `t('method1Time')`, `t('method1Price', { threshold: FREE_SHIPPING_THRESHOLD })`, etc. calls.

```tsx
const methods = [
  {
    title:  t('method1Title'),
    time:   t('method1Time'),
    price:  t('method1Price', { threshold: FREE_SHIPPING_THRESHOLD }),
    detail: t('method1Detail'),
  },
  { title: t('method2Title'), time: t('method2Time'), price: t('method2Price'), detail: t('method2Detail') },
  { title: t('method3Title'), time: t('method3Time'), price: t('method3Price'), detail: t('method3Detail') },
]
```

Replace the note box content: `t('noteStrong')` + `t('note')`.

- [ ] **Step 4: Translate Returns page**

Same pattern as Shipping using `getTranslations('Returns')` and individual `t('section1Title')` etc. keys.

- [ ] **Step 5: Translate Terms and Privacy pages**

Both use `getTranslations('Terms')` / `getTranslations('Privacy')`. Terms page replaces all section titles/details with `t('section1Title')` etc. Privacy page uses `t('heading')` and `t('eyebrow')`.

- [ ] **Step 6: Translate NotFound page**

```tsx
import { getTranslations } from 'next-intl/server'
import { Link } from '@/navigation'
import { BRAND } from '@/lib/constants'

export default async function NotFound() {
  const t = await getTranslations('NotFound')
  return (
    <div className="flex min-h-screen flex-col items-center justify-center bg-surface px-6 text-center">
      <p className="font-display text-[120px] leading-none text-border-subtle select-none">404</p>
      <h1 className="font-display text-4xl tracking-widest text-on-surface mb-4">{t('heading')}</h1>
      <p className="font-body text-sm tracking-widest text-on-surface-muted mb-10">
        {t('body')}
      </p>
      <Link
        href="/"
        className="border border-on-surface px-8 py-3 font-body text-[10px] tracking-[0.2em] uppercase text-on-surface transition-colors hover:bg-on-surface hover:text-surface"
      >
        {t('back', { brand: BRAND })}
      </Link>
    </div>
  )
}
```

- [ ] **Step 7: Translate SizeGuide and CheckoutPage**

`SizeGuide` page: use `getTranslations('SizeGuide')` and replace heading, eyebrow, intro text, measure instructions, and tab labels.

`CheckoutPage` (server route wrapper): add `getTranslations('CheckoutPage')` and pass `t('title')` to the client component as a prop.

- [ ] **Step 8: Update test files for pages**

For page tests that use `render(await Page())`, add mock for `next-intl/server`:

```tsx
vi.mock('next-intl/server', () => ({
  getTranslations: async () => (key: string, values?: Record<string, unknown>) => {
    if (!values) return key
    return Object.entries(values).reduce(
      (str, [k, v]) => str.replace(`{${k}}`, String(v)),
      key,
    )
  },
  getMessages: async () => ({}),
  getLocale:   async () => 'en',
}))
```

Update assertions that checked hardcoded strings to match the key name (e.g., `'Return Policy'` → `'section1Title'`), OR use the real `en.json` values by importing and checking against them.

Recommended: keep assertions checking the key string (simpler mock), except for tests that specifically test the text content.

- [ ] **Step 9: Run all tests**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test 2>&1 | tail -30
```

- [ ] **Step 10: Commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add "src/app/[locale]/" && git commit -m "feat(i18n): translate all info pages, NotFound, SizeGuide, Checkout"
```

---

## Task 17: Final verification

- [ ] **Step 1: TypeScript strict check**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npx tsc --noEmit
```

Expected: 0 errors.

- [ ] **Step 2: ESLint zero warnings**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npx eslint src --max-warnings=0
```

Expected: 0 warnings.

- [ ] **Step 3: All tests pass**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm test
```

Expected: all tests pass.

- [ ] **Step 4: Production build**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm run build
```

Expected: successful build.

- [ ] **Step 5: Manual smoke test**

Start dev server:
```bash
cd "C:/Users/denno/Desktop/Norelia project" && npm run dev
```

Check:
1. `http://localhost:3000` — Greek UI, navbar shows ΑΝΔΡΑΣ / ΓΥΝΑΙΚΑ, display font is Oswald
2. `http://localhost:3000/en` — English UI, navbar shows Men / Women, display font is Bebas Neue
3. Flag switcher in navbar switches locale
4. `/en/men` shows English Men page
5. Cookie banner shows Greek text by default
6. Cart and favorites work in both locales
7. All info pages render correctly in both languages

- [ ] **Step 6: Final commit**

```bash
cd "C:/Users/denno/Desktop/Norelia project" && git add -A && git commit -m "feat(i18n): Greek/English bilingual support complete with next-intl"
```
